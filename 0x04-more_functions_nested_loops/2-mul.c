#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: The firsst number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplicationbetween x and y para
 */

int mul(int x, int y)
{
	return (x * y);
}
